"use client";

/**
 * Portal dashboard landing page.
 *
 * Intentionally blank — it is the permission fallback route. The portal layout
 * still renders the permission-gated sidebar/menu around this page, so menu
 * permissions continue to apply.
 */
export default function DashboardPage() {
  return <div className="p-6" />;
}
