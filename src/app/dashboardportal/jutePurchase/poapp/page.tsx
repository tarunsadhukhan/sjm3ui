/**
 * Jute Purchase Order (App) — menu variant route.
 * Re-exports the Jute PO index page; navigation inside the page is
 * path-aware, so create/edit/view stay under /poapp.
 */
export { default } from "../po/page";
